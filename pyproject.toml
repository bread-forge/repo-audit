[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "repo-audit"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/repo_audit"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 99

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.4",
]
