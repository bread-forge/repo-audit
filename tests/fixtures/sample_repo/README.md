# Sample Repository

A minimal Python package used as a test fixture for repo-audit.

## Overview

This fixture contains a `sample` package with:

- `core.py` — computation logic
- `__main__.py` — package entry point
