# Sample Specification

This spec describes the behaviour of the sample package fixture.

## Compute Function

The `compute` function doubles its integer input and returns the result.

## Platform Function

The `get_platform` function returns the current platform string from `sys.platform`.
